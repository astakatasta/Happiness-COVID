---
title: "La felicità negli anni del COVID-19"
author: "Ilaria Fenos"
date: "2022-08-13"
output: html_document
---

```{r setup, include=FALSE}
library(tidyverse)
library(lubridate)
library(readr)
library(DescTools)
library(countrycode)
library(hablar)
require(maps)
library(tmap)
library(gganimate)

knitr::opts_chunk$set(echo = TRUE)
```

## Introduzione
Il *World Happiness Report* è una pubblicazione del Sustainable Development Solutions Network organizzato dal Gallup World Poll data he contiene articoli e classifiche della felicità nazionale dei vari stati del mondo, sulla base di interviste, e la mette mette anche in correlazione con vari fattori della vita come:
- GDP per capita
- salute
- supporto sociale
- libertà
- fiducia nel governo
- generosità
percepiti dalle persone verso la propria comunità/nazione.

I report coinvolgono 153 stati e sono stati reperiti su Kaggle al seguente link [World Happiness Report up to 2022](https://www.kaggle.com/datasets/mathurinache/world-happiness-report?resource=download&select=2015.csv)

## Importazione dei dati
```{r datasets}
df_2015 = read.csv("datasets/2015.csv")
df_2016 = read.csv("datasets/2016.csv")
df_2017 = read.csv("datasets/2017.csv")
df_2018 = read.csv("datasets/2018.csv")
df_2019 = read.csv("datasets/2019.csv")
df_2020 = read.csv("datasets/2020.csv")
df_2021 = read.csv("datasets/2021.csv")
df_2022 = read.csv("datasets/2022.csv")
```

## Normalizzazione dei dati
Il seguente codice serve per normalizzare i dati dei singoli dataframe per poi includerli in un unico dataset con cui lavorare.

```{r tidy}
#dfList <- list(df_2016, df_2017, df_2018, df_2019, df_2020, df_2021, df_2022)

df_2020 = df_2020[,-(4:13)]
df_2021 = df_2021[,-(4:13)]

tidyDf = function(df) {
  #aggiungo la colonna anno
  year = substr(substitute(df),4,7)
  df$Year = year
  
  #tolgo alcuni attributi che non mi interessano come l'errore standard e il residuo distopico
  drops = c("Standard", "Whisker", "Dystopia", "Lower", "Upper", "Rank")
  
  df = df %>% select(-contains(drops))
  
  #rinonimo la colonna "Country"
  colnames(df)[which(names(df) %like% "%ountry%")] <- "Country"
  
  #rinonimo la colonna "Score"
  colnames(df)[which(names(df) %like% "%core%")] <- "Score"
  
  #rinonimo la colonna "GDP per capita"
  colnames(df)[which(names(df) %like% "%GDP%")] <- "GDP per capita"
  
  #rinonimo la colonna "Social support"
  colnames(df)[which(names(df) %like% "%Family%")] <- "Social support"
  colnames(df)[which(names(df) %like% "%ocial%")] <- "Social support"
  
  #rinonimo la colonna "Generosity"
  colnames(df)[which(names(df) %like% "%enerosity%")] <- "Generosity"
  
  #rinonimo la colonna "Trust in goverment"
  colnames(df)[which(names(df) %like% "%orruption%")] <- "Trust in goverment"
  
  #rinonimo la colonna "Health"
  colnames(df)[which(names(df) %like% "%ealth%")] <- "Health"
  
  #rinonimo la colonna "Freedom"
  colnames(df)[which(names(df) %like% "%reedom%")] <- "Freedom"
  
  df = df %>% select(-contains("region")) %>% arrange(desc(Score))
  
  df = df %>% convert(num(Score:Year))

}

#applicare la funzione per ogni dataframe
df_2015 = tidyDf(df_2015)
#view(df_2015)
df_2016 = tidyDf(df_2016)
#view(df_2016)
df_2017 = tidyDf(df_2017)
#view(df_2017)
df_2018 = tidyDf(df_2018)
#view(df_2018)
df_2019 = tidyDf(df_2019)
#view(df_2019)
df_2020 = tidyDf(df_2020)
#view(df_2020)
df_2021 = tidyDf(df_2021)
#view(df_2021)
df_2022 = tidyDf(df_2022)
#view(df_2022)

#unione di tutti i dataframe
df_union = union(df_2015, df_2016)
df_union = union(df_union, df_2017)
df_union = union(df_union, df_2018)
df_union = union(df_union, df_2019)
df_union = union(df_union, df_2020)
df_union = union(df_union, df_2021)
df_union = union(df_union, df_2022)
df_union = df_union %>% arrange(desc(Score))
#view(df_union)

#view(df_union[rowSums(is.na(df_union))>0,])
#row 165 Trust = NA

#controllo gli stati che appaiono più volte con nomi diversi
#view(df_union %>% count(Country) %>% arrange(n) %>% filter(n < 8) %>% arrange(Country))

#sistemo i nomi
df_union = df_union %>% mutate(Country = case_when(
  Country == "Azerbaijan*" ~ "Azerbaijan",
  Country == "Belarus*" ~ "Belarus", 
  Country == "Botswana*" ~ "Botswana",
  Country == "Chad*" ~ "Chad",
  Country == "Comoros*" ~ "Comoros",
  Country == "Congo (Kinshasa)" ~ "Congo, Democratic Republic of the",
  Country == "Congo (Brazzaville)" ~ "Congo",
  Country == "Czechia" ~ "Czech Republic",
  Country == "Eswatini, Kingdom of*" ~ "Eswatini",
  Country == "Swaziland" ~ "Eswatini",
  Country == "Guatemala*" ~ "Guatemala",
  Country == "Gambia*" ~ "Gambia",
  Country == "Hong Kong S.A.R., China" ~ "Hong Kong",
  Country == "Hong Kong S.A.R. of China" ~ "Hong Kong",
  Country == "Kuwait*" ~ "Kuwait",
  Country == "Lesotho*" ~ "Lesotho",
  Country == "Liberia*" ~ "Liberia",
  Country == "Libya*" ~ "Libya",
  Country == "Luxembourg*" ~ "Luxembourg",
  Country == "Madagascar*" ~ "Madagascar",
  Country == "Mauritania*" ~ "Mauritania",
  Country == "Niger*" ~ "Niger",
  Country == "Palestinian Territories*" ~ "Palestinian Territories",
  Country == "Rwanda*" ~ "Rwanda",
  Country == "Turkmenistan*" ~ "Turkmenistan",
  Country == "Yemen*" ~ "Yemen",
  Country == "North Macedonia" ~ "Macedonia",
  Country == "Northern Cyprus" ~ "North Cyprus",
  Country == "North Cyprus*" ~ "North Cyprus",
  Country == "Somaliland region" ~ "Somaliland",
  Country == "Somaliland Region" ~ "Somaliland",
  Country == "Taiwan Province of China" ~ "Taiwan",
  Country == "Trinidad & Tobago" ~ "Trinidad and Tobago",
  TRUE ~ as.character(Country)
))

#togliere stati con meno di 7 anni di informazioni tranne Comoros a cui manca 2017 e 2018
#less7 = df_union %>% count(Country) %>% filter(n<7) %>% filter(Country != "Comoros")
less7 = df_union %>% count(Country) %>% filter(n<8)
#print(df_union %>% count())
#1231
#print(sum(less7$n))
#71 righe da eliminare
df_less7 = filter(df_union, Country %in% less7$Country)
df_union = setdiff(df_union, df_less7)
#print(df_union %>% count())
#1160
df_union$iso_a3 = countrycode(sourcevar = df_union$Country, origin = "country.name", destination = "iso3c")
df_union = df_union %>% drop_na()
#print(df_union %>% count())
#1152
#view(df_union)
```
``` {r echo=FALSE, message=FALSE, comment="functions", results='hide'}
options(scipen = 999)

change_start_end = function(start, end) {
  df = df_union %>% filter(Year == start | Year == end)
  
  df = df %>%
    group_by(Country) %>%
    #mutate(Mean = mean(Score)) %>%
    select(Year, Country, Score) %>%
    filter(Year == start | Year == end) %>%
    pivot_wider(names_from = Year, values_from = Score) %>% 
    rowwise()
  
    df[[paste("Change", end)]] <- df[[as.character(end)]] - df[[as.character(start)]]
    
    df = df[,-(2:3)]
    
    df
}
```

## La felicità prima del COVID-19 (anni dal 2015 al 2020)
``` {r}
df_2015_2020 = df_union %>% filter(Year < 2021) %>% drop_na()

data("World")
World = World %>% select(iso_a3, continent)
World = left_join(df_union, World, by="iso_a3")

continents = World %>% select(Country, continent)

mean_2015_2020 = df_2015_2020 %>% group_by(Country) %>% summarise(Mean = mean(Score))

mean_2015_2020 = left_join(mean_2015_2020, a, by = "Country")

mean_2015_2020 = mean_2015_2020[!duplicated(mean_2015_2020),]

mean_2015_2020 %>% 
  arrange(desc(Mean)) %>% 
  slice_head(n = 20) %>% 
  ggplot(aes(x = reorder(Country, Mean),
             y = Mean,
             colour = continent)) +
  geom_col(width = 0.75) +
  coord_flip() +
  scale_fill_viridis_c(begin = 0, end = 0, option = "turbo") +
  guides(fill = "none") +
  labs(title = "Top 20 stati con punteggio di felicità più elevato (2015-2019)",
       x = "",
       y = "") +
  geom_text(aes(label = round(Mean, 2)),
            color = "white",
            hjust = 1.5,
            size = 3)
``` 
DIstribuzione felicità 2015-2020

``` {r}

```

``` {r}
change_2015_2020 = change_start_end(2015,2020)

colnames(change_2015_2020)[2] = "Change"

a = df_union %>% 
      filter(Year == 2015 | Year == 2020) %>% 
      select(Year, Country, Score) %>%
      pivot_wider(names_from = Year, values_from = Score) %>% 
      rowwise()

change_2015_2020 = left_join(change_2015_2020, a, by = "Country")

change_2015_2020 = change_2015_2020[c("Country", "2015", "2020", "Change")]

data("World")
World = World %>% select(iso_a3, continent)
World = left_join(df_union, World, by="iso_a3")

a = World %>% select(Country, continent)

change_2015_2020 = left_join(change_2015_2020, a, by = "Country")

change_2015_2020 = change_2015_2020[!duplicated(change_2015_2020),]
``` 

## cambiamento ogni anno

``` {r}
change_2015_2016 = change_start_end(2015,2016)
#print(mean(change_2015_2016$Change))
change_2016_2017 = change_start_end(2016,2017)
#print(mean(change_2016_2017$Change))
change_2017_2018 = change_start_end(2017,2018)
#print(mean(change_2017_2018$Change, na.rm = TRUE))
change_2018_2019 = change_start_end(2018,2019)
#print(mean(change_2018_2019$Change, na.rm = TRUE))
change_2019_2020 = change_start_end(2019,2020)
#print(mean(change_2019_2020$Change))
change_2020_2021 = change_start_end(2020,2021)
#print(mean(change_2020_2021$Change))
change_2021_2022 = change_start_end(2021,2022)
#print(mean(change_2021_2022$Change))
change_by_year = left_join(change_2015_2016, change_2016_2017, by= "Country")
change_by_year = left_join(change_by_year, change_2017_2018, by= "Country") 
change_by_year = left_join(change_by_year, change_2018_2019, by= "Country") 
change_by_year = left_join(change_by_year, change_2019_2020, by= "Country") 
change_by_year = left_join(change_by_year, change_2020_2021, by= "Country") 
change_by_year = left_join(change_by_year, change_2021_2022, by= "Country")

names(change_by_year) <- substring(names(change_by_year), 8)
change_by_year = gather(change_by_year, 2:8, key = "Year", value = "Change")
colnames(change_by_year)[1] = "Country"
view(change_by_year)
```

Che cambiamenti ha avuto il punteggio di felicità nel mondo fra il 2015 e il 2020

``` {r}
df_2015_2019 = df_union %>% filter(Year < 2020)

country_score_change_2015_2019 <- df_2015_2019 %>% 
  group_by(Country) %>%
  mutate(Mean = mean(Score)) %>%
  #count(n() == 5) %>%
  select(Year, Country, Score, Mean) %>%
  #mutate(as.integer(Year)) %>%
  filter(Year == "2015" | Year == "2019") %>% 
  pivot_wider(names_from = Year,
              values_from = Score) %>% 
  rowwise() %>%
  mutate(Change = (`2019` - `2015`)) %>%
  #select(-c(`2015`, `2019`)) %>%
  arrange(-Mean)
```

Numero di stati che hanno avuto un incremento della felicità

``` {r}
positive_2015_2019 = country_score_change_2015_2019 %>% filter(Change > 0)
show(nrow(positive_2015_2019))
```

Numero di stati che hanno avuto un decremento della felicità:

``` {r}
negative_2015_2019 = country_score_change_2015_2019 %>% filter(Change < 0)
show(nrow(negative_2015_2019))
```

Medie dei cambiamenti dello score della felicità tra il 2015 e il 2019

``` {r}
show("Nel totale: ")
show(mean(country_score_change_2015_2019$Change))
show("In positivo:")
show(mean(positive_2015_2019$Change))
show("In negativo:")
show(mean(negative_2015_2019$Change))
```

Correlazione fra il punteggio di felicità e i fattori che la influenzano nel periodo 2015-2020

``` {r}
cor_2015_2019 <- round(
  cor(df_2015_2019 %>% 
      drop_na() %>% 
      select(-c(Year, Country, iso_a3))
    ), 2)

col <- colorRampPalette(c("#BB4444", "#EE9988", "#FFFFFF", "#77AADD", "#4477AA"))

# Plot correlation matrix
corrplot(cor_2015_2019,
         method = "shade",
         shade.col = NA,
         tl.col = "black",
         tl.srt = 45,
         col = col(200),
         addCoef.col = "black",
         cl.pos = "n",
         order = "AOE",
         type = "upper")
```

## Felicità dopo l'avvento del COVID-19

```{r}
data("World")
World = World %>% select(iso_a3, continent)
World = left_join(df_union, World, by="iso_a3")

avg_by_year = df_union %>% group_by(Year) %>% summarise(Score = mean(Score))
a = df_union %>% group_by(Year) %>% summarise(GDP = mean(`GDP per capita`))
avg_by_year$GDP = a$GDP
a = df_union %>% group_by(Year) %>% summarise(Social = mean(`Social support`))
avg_by_year$SocialSupport = a$Social
a = df_union %>% group_by(Year) %>% summarise(Health = mean(Health))
avg_by_year$Health = a$Health
a = df_union %>% group_by(Year) %>% summarise(Freedom = mean(Freedom))
avg_by_year$Freedom = a$Freedom
a = df_union %>% group_by(Year) %>% summarise(Trust = mean(`Trust in goverment`))
avg_by_year$Trust = a$Trust
a = df_union %>% group_by(Year) %>% summarise(Generosity = mean(Generosity))
avg_by_year$Generosity = a$Generosity

for (x in c(2:8)) {
  avg_by_year[x,"Change"] = avg_by_year[x,"Score"] - avg_by_year[x-1,"Score"]
}
avg_by_year$Change = avg_by_year$Change * 100
avg_by_year$Change = trunc(avg_by_year$Change*10^2)/10^2
avg_by_year$Change = as.character(avg_by_year$Change)
avg_by_year$Change = paste(avg_by_year$Change, " %")
avg_by_year[1,"Change"] = ""
  
ggplot(avg_by_year, aes(x=Year, y=Score)) +
    geom_line(size=2, color = "red") +
    geom_text(aes(label=Change), fontface='bold' ,size=3.5, position = position_nudge(x=0, y=0.02)) +
    ggtitle("Andamento medio della felicità dal 2015 al 2022") +
    theme_bw(base_size=15) +
    theme(panel.background = element_rect(fill = "lightyellow")) +
    ylab("Punteggio") +
    xlab("Anno")
```

``` {r}
mean_by_country = World %>% group_by(Country) %>% 
                  summarise(Mean = mean(Score))

mean_by_country$Country = factor(mean_by_country$Country, 
                                  levels = mean_by_country$Country[
                                    order(mean_by_country$Mean, decreasing = TRUE)])

mean_by_country %>% ggplot(aes(x=Country, y=Mean)) +
  geom_bar(stat = "identity") + 
  theme(axis.text.x=element_blank()) +
  geom_line(aes(x = as.numeric(Country), y = Mean), color = "red", size = 2.5)

``` 
``` {r}
mean_by_continent = World %>% group_by(continent) %>% 
                  summarise(Mean = mean(Score))
mean_by_continent$continent = factor(mean_by_continent$continent, 
                                  levels = mean_by_continent$continent[
                                    order(mean_by_continent$Mean, decreasing = TRUE)])

mean_by_continent %>%
  ggplot(aes(x = continent, y = Mean, fill = continent)) +
  geom_bar(stat = "identity")
``` 

``` {r echo=FALSE, message=FALSE, comment="continenti", results='hide'}
World = World %>% drop_na()

avg_continent_by_year = World %>% group_by(Year, continent) %>% summarise(Mean = mean(Score))

avg_continent_by_year %>% ggplot(aes(Year, Mean)) +
            geom_line(aes(group = continent), alpha = 2) + 
            facet_wrap(~continent)  +
            theme_classic()
```

``` {r echo=FALSE, message=FALSE, comment="barplot", results='hide'}
factors_by_year = avg_by_year %>% select(-Score)
factors_by_year = gather(factors_by_year, 'GDP':'Generosity', key = "Factor", value = "Mean")
transform(factors_by_year, Year = as.integer(Year))
ggplot(factors_by_year, aes(x=Factor, y=Mean, fill=Factor)) + 
  geom_bar(stat='identity', show.legend = FALSE) +
  theme_bw() +
  # gganimate specific bits:
  transition_states(
    Year,
    transition_length = 2,
    state_length = 1
  ) +  
  labs(title = 'Year: {closest_state}', x = 'Fattori che influenzano la felicità', y = 'Punteggio medio') +
  ease_aes('sine-in-out')

anim_save("barplot-factor-by-year.gif")
```

``` {r echo=FALSE, message=FALSE, comment="score gif", results='hide'}
data("World")
World = left_join(World, df_union, by="iso_a3")

score_ap <- 
  tm_shape(World) +
  tm_facets(along = "Year", free.coords = FALSE, showNA = FALSE) +
  tm_style("col_blind") +
  tm_fill("Score", title="Felicità nel mondo dal 2015 al 2022", style = "fixed",
        breaks = c(0, 2.5, 3.5, 4.5, 5.5, 6, 6.5, 7, 7.5, Inf),
        palette = c("#BA0000", "red", "#FF4500", "orange", "yellow", "#32CD32", "#228B22", "#008000", "#006400"),
        colorNA = "gray") +
  tm_borders()

#creazione gif
tmap_animation(world_anim, filename = "score_by_year.gif", delay = 200, width = 1000, height = 620)
```

Animazione punteggio della felicità per paese dal 2015 al 2022

```{r}
knitr::include_graphics("score_by_year.gif")
```

## Impatto nei primi mesi (analisi dell'anno 2020) 

``` {r}
df_2019_2020 = df_union %>% filter(Year == "2019" | Year == "2020")

country_score_change_2019_2020 <- df_2019_2020 %>% 
  group_by(Country) %>%
  mutate(Mean = mean(Score)) %>%
  select(Year, Country, Score, Mean) %>%
  pivot_wider(names_from = Year,
              values_from = Score) %>% 
  rowwise() %>%
  mutate(Change = (`2020` - `2019`)) %>%
  #select(-c(`2019`, `2020`)) %>%
  arrange(-Mean)
```

Numero di stati che hanno avuto un incremento della felicità

``` {r}
positive_2019_2020 = country_score_change_2019_2020 %>% filter(Change > 0)
show(nrow(positive_2019_2020))
```

Numero di stati che hanno avuto un decremento della felicità:

``` {r}
negative_2019_2020 = country_score_change_2019_2020 %>% filter(Change < 0)
show(nrow(negative_2019_2020))
```

Medie dei cambiamenti dello score della felicità tra il 2015 e il 2019

``` {r}
show("Nel totale: ")
show(mean(country_score_change_2019_2020$Change))
show("In positivo:")
show(mean(positive_2019_2020$Change))
show("In negativo:")
show(mean(negative_2019_2020$Change))
```

Correlazione fra il punteggio di felicità e i fattori che la influenzano nel periodo 2015-2019

``` {r}
cor_2019_2020 <- round(
  cor(df_2019_2020 %>% 
      drop_na() %>% 
      select(-c(Year, Country, iso_a3))
    ), 2)

# Plot correlation matrix
corrplot(cor_2019_2020,
         method = "shade",
         shade.col = NA,
         tl.col = "black",
         tl.srt = 45,
         col = col(200),
         addCoef.col = "black",
         cl.pos = "n",
         order = "AOE",
         type = "upper")
```

## Andamento negli anni successivi
``` {r}
#Burundi e Haiti hanno solo 1 anno
df_2021_2022 = df_union %>% filter(Year > 2020) %>% filter(Country != "Burundi", Country != "Haiti")

country_score_change_2021_2022 <- df_2021_2022 %>% 
  group_by(Country) %>%
  mutate(Mean = mean(Score)) %>%
  select(Year, Country, Score, Mean) %>%
  pivot_wider(names_from = Year,
              values_from = Score) %>% 
  rowwise() %>%
  mutate(Change = (`2021` - `2022`)) %>%
  #select(-c(`2021`, `2022`)) %>%
  arrange(-Mean)
```

Numero di stati che hanno avuto un incremento della felicità

``` {r}
positive_2021_2022 = country_score_change_2021_2022 %>% filter(Change > 0)
show(nrow(positive_2021_2022))
```

Numero di stati che hanno avuto un decremento della felicità:

``` {r}
negative_2021_2022 = country_score_change_2021_2022 %>% filter(Change < 0)
show(nrow(negative_2021_2022))
```

Medie dei cambiamenti dello score della felicità tra il 2021 e il 2022

``` {r}
show("Nel totale: ")
show(mean(country_score_change_2021_2022$Change))
show("In positivo:")
show(mean(positive_2021_2022$Change))
show("In negativo:")
show(mean(negative_2021_2022$Change))
```

Correlazione fra il punteggio di felicità e i fattori che la influenzano nel periodo 2015-2019

``` {r}
cor_2021_2022 <- round(
  cor(df_2021_2022 %>% 
      drop_na() %>% 
      select(-c(Year, Country, iso_a3))
    ), 2)

# Plot correlation matrix
corrplot(cor_2021_2022,
         method = "shade",
         shade.col = NA,
         tl.col = "black",
         tl.srt = 45,
         col = col(200),
         addCoef.col = "black",
         cl.pos = "n",
         order = "AOE",
         type = "upper")
```
``` {r}
country_union = country_score_change_2015_2019 %>% select(Country, Change) %>% filter(Country != "Burundi", Country != "Haiti")
country_union = rename(country_union, prima = Change)
a = country_score_change_2019_2020 %>% filter(Country != "Burundi", Country != "Haiti", Country != "Namibia")
country_union$durante = a$Change
a = country_score_change_2021_2022 %>% filter(Country != "Namibia")
country_union$dopo = a$Change
#view(country_union)
country_union = gather(country_union, 'prima', 'durante', 'dopo', key = "periodo", value = "diff")

df_union %>% ggplot(aes(x = Year, y = Score)) +
  geom_boxplot(aes(group=Year))
```
``` {r}

```

## Conclusioni
C'è stato un cambiamento significativo nella felicità fra gli anni prima del covid e il 2020? E negli anni successivi? no

